<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Đăng bài viết</title>
    <%- include('../layout/head') -%>
    <script src="https://cdn.tiny.cloud/1/no-api-key/tinymce/5/tinymce.min.js" referrerpolicy="origin"></script>
    <script>tinymce.init({selector:'textarea'});</script>
</head>
<body>
    <div class="container">
        <%- include('../layout/nav') -%>
           
        <div class="panel panel-default">
            <div class="panel-heading">
               Thêm bài mới
            </div>
            <div class="panel-body">
                <div class="row">
                    <div class="col-lg-6">
                        <% if(data && data.error) {%>
                            <div class="alert alert-danger col-sm-12">
                                <%= data.error%>
                            </div>
                        <%}%>
                        <form role="form" method="POST" action="/admin/post/store">
                            <div class="form-group">
                                <label>Tiêu đề bài viết</label>
                                <input class="form-control" name="title" placeholder="Nhập tiêu đề bài viết">
                            </div>
                            
                            <div class="form-group">
                                <label>Nội dung</label>
                                <textarea name="content" class="form-control" rows="3" placeholder="Nội dung bài viết"></textarea>
                            </div>
                            <!-- <div class="form-group">
                                <label>Tiêu đề bài viết</label>
                                <input class="form-control" name="content" placeholder="Nhập tiêu đề bài viết">
                            </div> -->
                            
                            <div class="form-group">
                                <label>Tác giả</label>
                                <input class="form-control" name="author" placeholder="Tên tác giả">
                            </div>
                            <input type="submit" name="" value="Thêm bài viết">
                        </form>
                    <!-- /.col-lg-6 (nested) -->
                </div>
                <!-- /.row (nested) -->
            </div>
            <!-- /.panel-body -->
        </div>

    </div>
    
</body>
</html>